services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - uploads:/app/uploads
      - dbdata:/app/data
    restart: unless-stopped

volumes:
  uploads:
  dbdata:
